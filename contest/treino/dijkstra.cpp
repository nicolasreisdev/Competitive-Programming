#include <bits/stdc++.h>
using namespace std;

#define pb push_back
#define rm pop_back
#define f first
#define s second

typedef pair<int, int> p;
typedef long long ll;
const int MAX = 1e5 + 10;

void dijkstra(int x){
    priority_queue<pair<int,int>>pq; 
    pq.push({-0,x});
    while(!pq.empty()){
        int u = pq.front().second, d = -pq.front().first;
        pq.pop();
        if(d > )
    }

}

int main(){



    return 0;
}