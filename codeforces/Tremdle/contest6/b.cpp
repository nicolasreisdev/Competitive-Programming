#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define fastio ios_base::sync_with_stdio(0);cin.tie(0);

typedef long long ll;

int main(){
    fastio;

    int t; cin >> t;
    while(t--){
        int n, k; cin >> n >> k;
        vector<int> memoria, ram;
        for(int i = 0; i < n;i++){
            int x; cin >> x; memoria.push_back(x);
        }
        for(int i = 0; i < n;i++){
            int x; cin >> x; ram.push_back(x);
        }
    }

    return 0;
}