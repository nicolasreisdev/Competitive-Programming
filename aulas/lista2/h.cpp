#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, m, t, mult; cin >> n;

    cin.ignore();
    for(int i = 0; i < n;i++){
        cin >> m;
        for(int j = 0; j < m;j++){
        }

    }

    return 0;
}